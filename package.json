{
    "private": true,
    "workspaces": [
      "Source/*"
    ],
    "scripts": {
      "clean": "yarn workspaces run clean",
      "build": "yarn workspaces run build",
      "test": "yarn workspaces run test",
      "test:run": "yarn workspaces run test:run"
    },
    "devDependencies": {
        "@dolittle/typescript.build": "^1.0.0"
    }
  }
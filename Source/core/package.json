{
  "name": "@dolittle/core",
  "version": "1.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "transpile": "rm -rf dist && BABEL_ENV=production npx babel . --ignore node_modules,**/for_* -d dist",
    "prepublish": "yarn run transpile"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0"
  }
}